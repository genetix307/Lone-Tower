// Genetix Studio
if store.gems >= store.cost_attack_speed {image_index = store.color_scheme} else {image_index = 0}
if store.base_attack_speed + store.attack_speed >= stat_cap {image_index = 0 maxxed = 1}