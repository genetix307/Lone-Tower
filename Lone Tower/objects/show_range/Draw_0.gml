// Genetix Studio
draw_set_alpha(show_alpha)
draw_set_color(c_aqua)
draw_set_circle_precision(64);
draw_circle(tower.x,tower.y,hud.attack_range+14,true)
draw_circle(tower.x,tower.y,hud.attack_range+12,true)
draw_circle(tower.x,tower.y,hud.attack_range+10,true)
draw_set_color(c_teal)
draw_circle(tower.x,tower.y,hud.attack_range+8,true)
draw_circle(tower.x,tower.y,hud.attack_range+9,true)
draw_set_alpha(1)